apiVersion: v1
kind: Service
metadata:
  name: backend-node-svc
  namespace: soccerize-app
spec:
  selector:
    app: backend-node
  ports:
    - name: http
      protocol: TCP
      port: 5000        
      targetPort: 5000  
  type: ClusterIP      
